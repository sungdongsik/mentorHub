# mentorHub
멘토가 멘티를 찾는 커뮤니케이션!

수정하기!!!

# docker 명령어
- docker-compose down -v(기존 컨테이너가 있다면 정리)
- docker-compose up -d --build(이미지 빌드 및 컨테이너 실행)
- docker-compose ps(컨테이너 상태 확인)
- docker-compose logs -f(로그 확인)